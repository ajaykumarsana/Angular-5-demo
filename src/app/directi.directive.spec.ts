import { DirectiDirective } from './directi.directive';

describe('DirectiDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiDirective();
    expect(directive).toBeTruthy();
  });
});
